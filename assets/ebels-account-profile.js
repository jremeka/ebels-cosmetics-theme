/* ==========================================================================
   EBELS ACCOUNT — Profile tab
   Form submission itself is native (standard Shopify {% form 'customer' %}
   POST + redirect) — no AJAX needed. This file is a placeholder for future
   polish (e.g. a "Saved!" toast) once the form is confirmed working live.
   ========================================================================== */

(function () {
  'use strict';
  // Intentionally minimal for now — see comment above.
})();
